FROM php:8.2-fpm

# Install dependencies
RUN apt-get update && apt-get install -y \
    git \
    curl \
    libpng-dev \
    libonig-dev \
    libxml2-dev \
    libzip-dev \
    libicu-dev \
    libxslt-dev \
    libfreetype6-dev \
    libjpeg62-turbo-dev \
    zip \
    unzip \
    default-mysql-client \
    && docker-php-ext-configure gd --with-freetype --with-jpeg \
    && docker-php-ext-install -j$(nproc) \
        pdo_mysql \
        mysqli \
        mbstring \
        exif \
        pcntl \
        bcmath \
        gd \
        intl \
        soap \
        xsl \
        zip \
        sockets \
    && pecl install redis \
    && docker-php-ext-enable redis \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

# Install Composer
COPY --from=composer:latest /usr/bin/composer /usr/bin/composer

# Set PHP memory limit
RUN echo "memory_limit=2G" > /usr/local/etc/php/conf.d/memory.ini
RUN echo "max_execution_time=1800" >> /usr/local/etc/php/conf.d/memory.ini
RUN echo "realpath_cache_size=10M" >> /usr/local/etc/php/conf.d/memory.ini
RUN echo "realpath_cache_ttl=7200" >> /usr/local/etc/php/conf.d/memory.ini

WORKDIR /var/www/html

# Install script to run on first boot
COPY install-magento.sh /usr/local/bin/install-magento.sh
RUN chmod +x /usr/local/bin/install-magento.sh

EXPOSE 9000

CMD ["php-fpm"]
